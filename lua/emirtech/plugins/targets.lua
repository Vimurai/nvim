return {
	"wellle/targets.vim",
	-- targets.vim is a pure Vimscript plugin and generally works by extending Vim's
	-- built-in text object system. It usually doesn't require a `config` function
	-- in Lua, as it integrates directly with Vim's grammar.
}
