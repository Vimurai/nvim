# GEMINI.md — Bootstrap Shim → ARCHITECT.md

> Role rulefiles are decoupled from the CLI vendor (D-050 / E-183). The canonical
> Architect bootloader is **ARCHITECT.md**. This file exists only so a host that
> auto-loads `GEMINI.md` still bootstraps the Architect persona by importing it
> below. Edit `ARCHITECT.md`, never this shim. (The Gemini CLI is deprecated;
> the Architect role now defaults to the `agy` provider.)

@ARCHITECT.md
