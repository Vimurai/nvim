# AGENTS.md — Bootstrap Shim → ARCHITECT.md (Antigravity / `agy` workspace)

> Role rulefiles are decoupled from the CLI vendor (D-050 / E-183). The canonical
> Architect bootloader is **ARCHITECT.md** at the project root. This file lives in
> the `.agents/` workspace so Antigravity (`agy`) auto-loads the Architect persona
> and does NOT drift out of role — e.g. writing source code, which the ANTI-DRIFT
> Protocol (§35) reserves for the Engineer. Edit `ARCHITECT.md`, never this shim. (E-195)

@ARCHITECT.md
