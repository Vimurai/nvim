# Copilot Instructions — [Project Name]

<!--
  This file is read automatically by GitHub Copilot Chat.
  Generated by: ai init (AI-OS v3.2)
  Keep this file committed to the repo so all contributors share the same Copilot context.
  Fill in every section — incomplete sections mean Copilot will make assumptions.
-->

## Project overview

<!-- One sentence: what this project does and who it serves. -->
<!-- Example: "B2B SaaS platform for invoice management — serves SMB customers in the EU." -->

## Tech stack

| Layer      | Technology                        |
|------------|-----------------------------------|
| Frontend   | <!-- React 18 / Vue 3 / etc -->   |
| Backend    | <!-- Node.js + Express / Django / Spring Boot --> |
| Database   | <!-- PostgreSQL / MongoDB / DynamoDB --> |
| Auth       | <!-- JWT + Passport / Cognito / Auth0 --> |
| CI/CD      | <!-- GitHub Actions / CircleCI --> |
| Infra      | <!-- AWS / GCP / Azure + Terraform / CDK --> |

## Project context files (read these for deeper context)

- `.ai/DIGEST.md` — current architecture snapshot (if present)
- `.ai/ARCH.md` — module boundaries and API contracts
- `.ai/SECURITY.md` — security posture and threat model
- `.ai/BRIEF.md` — product requirements and constraints

## Coding conventions

<!-- Be specific. Vague conventions are ignored. -->
<!-- Examples: -->
<!--   - TypeScript strict mode; no `any` without a comment explaining why -->
<!--   - ESLint + Prettier; run `npm run lint` before committing -->
<!--   - Absolute imports from `src/`; no relative imports crossing module boundaries -->
<!--   - File naming: kebab-case for files, PascalCase for components -->
<!--   - Error handling: never swallow errors silently; always log + rethrow or handle -->

## Architecture constraints

<!-- What must not change without a decision review. -->
<!-- Examples: -->
<!--   - All API endpoints must go through the shared auth middleware at src/middleware/auth.ts -->
<!--   - Database queries only in the `repository/` layer — never in controllers or services -->
<!--   - No direct inter-service HTTP calls — use the event bus (see .ai/ARCH.md) -->
<!--   - The `shared/` package is used by 5+ services — changes require blast-radius analysis -->

## Security requirements (project-specific)

<!-- These ADD TO the global COPILOT.md security rules — they do not replace them. -->
<!-- Examples: -->
<!--   - All user-facing endpoints require JWT with role claim; use `requireRole()` middleware -->
<!--   - Data classified as PII must use the `encrypt()` utility from `src/lib/crypto.ts` -->
<!--   - File uploads: max 10MB, allowlist: [.pdf, .png, .jpg], store in S3 with pre-signed URLs -->
<!--   - All external API calls must go through `src/lib/http-client.ts` (adds tracing + retry) -->

## Testing requirements

<!-- Examples: -->
<!--   - Test framework: Jest + React Testing Library (frontend) / Supertest (backend) -->
<!--   - Coverage gate: PRs must not drop line coverage below 80% -->
<!--   - Every new API endpoint needs: happy path + 401 + 403 + validation error test -->
<!--   - E2E: Playwright; only smoke tests run in CI (full suite runs nightly) -->

## What Copilot must NOT do in this repo

<!-- Hard stops — violations block PR merge. -->
<!-- Examples: -->
<!--   - Do not modify files in `src/shared/auth/` without a security review ticket -->
<!--   - Do not add npm/pip/go dependencies without updating `.ai/DECISIONS.md` (D-### entry) -->
<!--   - Do not suggest GraphQL schema changes — those require coordination with the API team -->
<!--   - Do not change database migration files after they have been merged to main -->
<!--   - Do not add `console.log` in production code — use the structured logger at `src/lib/logger.ts` -->

## Preferred libraries (already approved for this project)

<!-- List approved libraries so Copilot doesn't suggest unapproved alternatives. -->
<!-- Examples: -->
<!--   - HTTP client: axios (pinned to ^1.6.0) -->
<!--   - Validation: Zod -->
<!--   - Date handling: date-fns (not moment, not dayjs) -->
<!--   - Testing utilities: @testing-library/react, msw for API mocking -->
<!--   - ORM: Prisma (schema at prisma/schema.prisma) -->
